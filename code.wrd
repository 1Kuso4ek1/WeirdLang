struct Name {
    fun Name(var name) ->
        this.name = name + "!!!"

    fun GetName() ->
        return name

    fun SetName(var name) ->
        this.name = name

    var name
}

fun main() {
    var p = Name("Hello")
    var f = Name("World")

    println(p.GetName(), ", ", f.GetName())
}
